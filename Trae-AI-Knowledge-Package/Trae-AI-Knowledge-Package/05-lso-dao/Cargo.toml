[package]
name = "lso-dao"
version = "0.1.0"
edition = "2021"
description = "LSO-DAO — 五境递进架构核心实现"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"
chrono = "0.4"
